---
"@dobesv/parquets": minor
---

Add `ParquetBufferWriter` for synchronous in-memory Parquet writing and `generateParquetBuffer` utility for producing a complete Parquet file as a `Buffer` in a single call.
